#include <stdio.h>
#include <string.h>
int main()
{
    char s1[]="keyur";
    char s2[]="chanchad family";
    char s3[10];
    // puts(strcat(s1,s2));
    // printf("The length of s1 is %d\n",strlen(s1));
    // printf("The length of s2 is %d\n",strlen(s2));
    // printf("The reverse string s1 is:");
    // puts(strrev(s1));
    // printf("The reverse string s2 is:");
    // puts(strrev(s2));
    // puts(strcpy(s3,strcat(s1,s2)));

    printf("%s is a friend of %s",s1,s2);
    return 0;
}